# =============================================================================
# GitHub Actions — Auto-deploy FinanceBot to server
# =============================================================================
# Triggers on push to `main` branch.
# Connects to your server via SSH and deploys the latest code.
#
# ── Required GitHub Secrets (Settings → Secrets and variables → Actions) ──
#
#   SERVER_HOST        — IP-адрес или домен сервера (например 123.45.67.89)
#   SERVER_USER        — SSH-пользователь (например root или deploy)
#   SERVER_SSH_KEY     — Приватный SSH-ключ (содержимое id_rsa / id_ed25519)
#   SERVER_PORT        — (опционально) SSH-порт, по умолчанию 22
#
# =============================================================================

name: Deploy FinanceBot

on:
  push:
    branches: [master]

jobs:
  deploy:
    name: 🚀 Deploy to Server
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout code ───────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Deploy via SSH ──────────────────────────────────────────────
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT || 22 }}
          script: |
            set -e

            APP_DIR="/root/Finance_bot"
            REPO_URL="git@github.com:${{ github.repository }}.git"
            BRANCH="master"

            echo "▸ Deploying $BRANCH to $APP_DIR …"

            # Clone or pull latest code
            if [ ! -d "$APP_DIR" ]; then
              git clone --branch "$BRANCH" "$REPO_URL" "$APP_DIR"
              cd "$APP_DIR"
            else
              cd "$APP_DIR"
              if [ ! -d ".git" ]; then
                git init
              fi
              git remote remove origin 2>/dev/null || true
              git remote add origin "$REPO_URL"
              git fetch origin "$BRANCH"
              git reset --hard "origin/$BRANCH"
              git clean -fd
            fi

            # Create virtual environment if missing
            if [ ! -d ".venv" ]; then
              python3 -m venv .venv
            fi

            # Install / update dependencies
            .venv/bin/pip install --upgrade pip
            .venv/bin/pip install -r requirements.txt

            # Update systemd service file
            if [ -f "finance-bot.service" ]; then
              echo "▸ Updating systemd service file ..."
              sudo cp finance-bot.service /etc/systemd/system/finance-bot.service
              sudo systemctl daemon-reload
              sudo systemctl enable finance-bot
            fi

            # Restart the bot (systemd service)
            sudo systemctl restart finance-bot

            echo "✓ Deploy complete"
