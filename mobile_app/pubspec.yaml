name: finance_tracker_app
description: A premium cross-platform mobile application client for the Finance Tracker bot.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  provider: ^6.1.2
  http: ^1.2.1
  google_fonts: ^6.2.1
  intl: ^0.20.2
  flutter_spinkit: ^5.2.1
  flutter_animate: ^4.5.0
  fl_chart: ^0.67.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.2.6
  share_plus: ^10.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
